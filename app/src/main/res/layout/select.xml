<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="300sp"
        android:layout_height="100sp"
        android:id="@+id/btn_friendlist"
        android:textSize="20dp"
        android:background="@drawable/friends_button"
        android:layout_above="@+id/btn_activitylist"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="89dp" />

    <Button
        android:layout_width="300sp"
        android:layout_height="100sp"
        android:background="@drawable/activity_button"
        android:id="@+id/btn_activitylist"
        android:textSize="20dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/btn_friendlist"
        android:layout_marginBottom="122dp" />
</RelativeLayout>